export var latex = {
    from: {
        polynomial: polynomialToLatex
    }
};
function polynomialToLatex(polynomial) {
    polynomial.sortMonomials();
    var sym = "x";
    var output = "";
    polynomial.monomials.forEach(function (monomial) {
        var sign = (monomial.coefficient < 0) ? "-" : "+";
        var co = Math.abs(monomial.coefficient);
        var deg = monomial.degree;
        if (monomial.degree >= 2)
            output += sign + co + "x^{" + deg + "}";
        if (monomial.degree === 1)
            output += sign + co + sym;
        if (monomial.degree === 0)
            output += sign + co;
    });
    if (output[0] === "+")
        output = output.slice(1);
    return output;
}
//# sourceMappingURL=latex.js.map