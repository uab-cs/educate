import * as math from "mathjs";
export default function solve(polynomial) {
    if (polynomial.highestDegree() !== 2)
        return null;
    if (polynomial.monomials.length !== 3)
        return null;
    var a = polynomial.getMonomialByDegree(2).coefficient;
    var b = polynomial.getMonomialByDegree(1).coefficient;
    var c = polynomial.getMonomialByDegree(0).coefficient;
    var plus = apply(a, b, c, math.add);
    var minus = apply(a, b, c, math.subtract);
    return { plus: plus, minus: minus };
}
function apply(a, b, c, method) {
    return math.divide(method(math.multiply(b, -1), math.sqrt(math.subtract(math.square(b), math.multiply(4, math.multiply(a, c))))), math.multiply(2, a));
}
//# sourceMappingURL=quadraticFormula.js.map